# Finite v203 — demo begins on the ordinary blank form

## Accepted opening scene

After the person chooses a browser surface, the live demo begins on the ordinary unsaved arrival form headed `What are you trying to make happen?` with the field `What do you want to plan?` visible.

Codex types the complete synthetic brief into that real field character by character, then pauses before submission. The demo does not replay the route-choice gateway as its first chapter.

## State boundary

- Existing saved plans and waiting arrivals remain intact.
- Demo entry projects a fresh unsaved arrival surface through `newPlanDraftMode` and `forceArrivalSurface`.
- No plan exists for the demo until Codex presses the visible `Build my rough plan` control after the person's Next click.

## Release proof

- Visible same-tab browser acceptance on the blank form.
- Updated handoff and routing regressions.
- Full automated suite, typecheck, production build, and deployment.

# Finite working identity

`finite-working-logo-source.png` is the exact ImageGen concept accepted by Benji on 2026-08-26 in Codex task `01a03b10-4409-7491-a8b8-84cde61f4e17`.

The accepted direction is the deep-forest lowercase `f`, a straight cyan connector with equal short arms, and one coral change node centered inside the stem. The lowercase `finite` wordmark uses the same mark with tight optical spacing.

`extract-accepted-logo.py` removes only the concept-sheet background and crops the accepted mark geometry into the transparent production assets in `public/`. It does not redraw or reinterpret the logo.
